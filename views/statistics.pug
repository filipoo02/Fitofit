extends base

block content
    .report-title
        h1 Monthly report
    table 
        tr 
            th Distance (km)
            th Date
        each res in results 
            tr 
                td= Math.round(res.distance * 100)/100 
                td= formatDate({dataInput: res.dateOfActivity,returnDate:true})